sale_prices = [
  100,
  83,
  220,
  40,
  100,
  400,
  10,
  1,
  3
]

#to sort this this but not replace the original variable
#we will want to prefor the actions below 
#run to see example play out


sorted_list = sorted(sale_prices)

print(sorted_list)
print(sale_prices)

#for backwards you will do the next steps

backwards_list = sorted(sale_prices, reverse=True)

print(backwards_list)