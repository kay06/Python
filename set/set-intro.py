tags = {
    'python', 
    'coding',
    'tutorials',
    'coding' #this will not show due to it not being unique
}

print(tags)

#nope to print(tags[0])

query_one = 'python' in tags
query_two = 'ruby' in tags

print(query_one)
print(query_two)