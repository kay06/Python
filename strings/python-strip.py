url = 'https://google.com'

print(url.strip('https://'))

#this function allows you toremove or 'strip' a sting out of another string

print(url.lstrip('https://'))

#starts from the left side and removes the string given then stops after found

print(url.rstrip('.com'))

#starts from the right and removes the string given then stops after found
